-> In this code, we check whether a number is an Armstrong number or not. An armstrong number is a number that is equal to the sum of its own digits each raised to the power of the number of digits.

#include <stdio.h>
#include <math.h>

int main() {
    int n, t, r, s = 0, c = 0;

    printf("Enter a number: ");
    scanf("%d", &n);

    t = n;

    while (t != 0) {
        t = t / 10;
        c++;
    }

    t = n;

    while (t != 0) {
        r = t % 10;
        s = s + pow(r, c);
        t = t / 10;
    }

    if (s == n)
        printf("%d is an Armstrong number.\n", n);
    else
        printf("%d is not an Armstrong number.\n", n);

    return 0;
}

-> Output:

Enter a number: 153
153 is an Armstrong number.
